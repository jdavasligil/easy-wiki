# Easy Wiki
A static site generation tool to easily create wiki content for personal use.

## Requirements
1. Node (Verified on v22.8.0)
2. pnpm package manager

## Install
`pnpm install`

## Usage
Run the file watcher with `pnpm start`.

Now, any edits made to the markdown will automatically be re-rendered in real
time.
